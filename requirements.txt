# GUST — Python-Abhängigkeiten
# PC:           pip install -r requirements.txt
# Raspberry Pi: pip install -r requirements.txt --break-system-packages

# Signalverarbeitung & Modulator
numpy
scipy

# Reed-Solomon FEC
reedsolo

# ECDSA-Authentifizierung (AUTH_EX 0x85/0x86, GUST-X / P8-12)
# cryptography     # auskommentiert — erst bei AUTH_EX-Implementierung nötig

# Audio I/O (Gateway-Betrieb)
sounddevice

# Web-Server (REST API + WebSocket)
aiohttp

# Raspberry Pi GPIO / PTT-Steuerung (nur auf RPi aktiv)
# RPi.GPIO         # auskommentiert — nicht installierbar auf PC

# Meshtastic-Integration (Phase 4)
# meshtastic       # auskommentiert — erst bei Gateway-Implementierung nötig

# MQTT (Phase 4)
# paho-mqtt        # auskommentiert — erst bei Gateway-Implementierung nötig

# I²C Sensoren / BME280 (nur RPi, Phase 4)
# smbus2           # auskommentiert — erst bei Hardware-Integration nötig